@AGENTS.md

## Claude Code notes

The instructions above are imported from `AGENTS.md`, the **canonical, tool-agnostic** instruction file for this repo — Claude Code loads it through this `@AGENTS.md` import, and Codex/other agents read it directly. Keep all repo-wide guidance in `AGENTS.md` so the two never drift; add only Claude-Code-specific notes here.

_(No Claude-specific overrides at the moment.)_
